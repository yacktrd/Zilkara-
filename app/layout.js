export const metadata = {
  title: "Zilkara",
  description: "Zilkara Market Scanner"
};

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  );
}

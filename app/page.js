export const dynamic = "force-dynamic";

export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Zilkara</h1>
      <p>Next.js is running.</p>
      <p>Try: <code>/api/state</code> and <code>/api/rebuild</code></p>
    </main>
  );
}

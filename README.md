# Xyvala

Xyvala is a decision intelligence API for crypto markets.

It transforms market data into structured decisions through a simple pipeline:

```text
scan → zones → decision → memory → evaluation
---

The goal is not to expose raw indicators only.

The goal is to expose structured market decisions.
